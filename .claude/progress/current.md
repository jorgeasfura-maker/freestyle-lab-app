# 📍 progress/current.md

| Campo | Valor |
|---|---|
| Fecha | 10-08-2026 |
| Feature en desarrollo | Rediseño visual: orden más profesional + personalización por familia (tipografía + ícono/motivo por familia en las 16 tarjetas, aprobado). Tarjetas más compactas + entrada escalonada al cerrar el splash (`body.cards-in`). El duelo del menú pasó por 3 iteraciones: (1) orbes de mic+anillos — rechazado, "se ve amateur"; (2) bocas SVG dibujadas a mano con fuego/rayo — rechazado, "muy básico"; (3) **actual**: `marca/bocas.svg` (ilustración real de 2 bocas que trajo el usuario, ~4MB/7000 paths) como `<img>`, sin fuego ni rayo — ahora son palabras del vocabulario (`flow`, `eco`, `rima`...) que salen de cada lado y se cruzan al centro (`.choque-palabras`, keyframes `cp-va-der`/`cp-va-izq`). **Pendiente**: confirmar visualmente con el usuario (no se pudo verificar por captura en esta sesión — el panel del navegador no estaba visible del lado de Claude) y evaluar si conviene convertir el SVG a PNG por tiempo de carga (tarda 1-4s en decodificar por su complejidad). Se probó y descartó un prototipo de "tarjeta compacta con hover" |
| Último commit | `b09b19f` (10-ago) — commiteado, sitio en vivo aún en el deploy anterior (falta `netlify deploy --prod`) |
| Sitio en vivo | https://freestyle-lab-app.netlify.app (deploy `6a807ee6c5eb6afe55225832`, 15-ago — commit `4ef005c`; md5 `cc6c44d75d802a03a0973ce0b88c78d3`). Anterior: `6a7fc27f37d951f17d359580`, 14-ago — logo oficial en 4 lugares + fix del `</div>` huérfano; md5 `8c0563b709181f4583aed9d0ae9d134e`). Anterior: `6a7fab79ccdba0e89f439a6e`, 14-ago — más motion (hover cards, accent-pulso, rec-pulso, fadeInView); md5 `0ae7ab513160025eea5637b0abebfb26`). Anterior: `6a7e8d4ab50dec7b1d88e159`, 14-ago — sin banner de beats, batalla sin marco con tipeo palabra a palabra; md5 `54d621adf57a6affd33a3c7a17ab3c09`). Anterior: `6a7e62d8770d41f4258edf07`, 13-ago — sin Modo Bestia ni Modo Principiante, y con el menú reagrupado en 5 secciones. Verificado por md5: local y producción idénticos (`18425ddfab71635d1c72c6af0736d920`) |
| Checkpoint de seguridad | `versiones/index-2026-08-10-antes-de-rediseno.html` guardado antes de tocar nada |
| Próximo paso | Continuar rediseño: páginas de detalle de técnica (trainer), modales (info/pistas), auth-bar — solo se hizo portada/menú/tarjetas hasta ahora. Nueva feature: pista de fondo al azar al grabar (`startPistaFondo`/`stopPistaFondo`, indicador `#pistaNow`) — reutiliza el array `PISTAS`, se salta en "tempo" (ya tiene su propio metrónomo) |
| Archivos modificados esta sesión | `index.html` (tokens --sp-*/--radius-* en :root, ritmo vertical de hero/modos/bestia-btn, `.fam-head` rediseñado con línea degradada, radios unificados en tcard/bestia-btn/pistas-banner/pista-row, clases `.fam-rima/.fam-voz/.fam-ingenio/.fam-disciplina/.fam-cuerpo` con tipografía propia + `.tcard-motif` ícono SVG por familia en las 16 tarjetas), `.claude/` (INDEX.md, HOT.md, este archivo, launch.json en esquirlafinal apunta a este proyecto) |
| Bloqueos | Ninguno |
| Técnica nueva (17ª) | **Rimas por Minuto** (`rimamin`, familia Rima, 60s fijos en los 3 modos). Mide VOLUMEN de rimas: agrupa las palabras por familia de rima sin importar cuánto texto haya en medio — a diferencia de `cadena`, que exige palabras consecutivas y se corta. Cada palabra extra en una familia suma 1. Repetir la misma palabra no suma. Piezas: `analyzeRimasMinuto()`, `TECH_INFO.rimamin`, `SEGS.rimamin`, `#cardRimaMin`, rama en `openTech`/`newItem`/`analizar`, entrada en `BADGE`, colores `--tc` en los 3 temas |
| Bug corregido (11-ago) | La técnica daba **0 rimas** en rondas llenas de rimas, por dos causas: (1) el criterio "últimas 2 vocales" no detectaba rimas agudas (`ejecutar`/`adaptar` no agrupaban) → nueva función global **`claveRima()`**, que corta desde la vocal tónica usando la regla de acentuación española (aguda si lleva tilde final o termina en consonante ≠ n/s) y maneja diptongos (`canción`≡`corazón`); (2) el reconocimiento del navegador entrega tramos DESPUÉS de `stop()`, así que `#edit` se quedaba con el recorte final mientras `#live` mostraba todo, y se analizaba el recorte → **`sincronizarEdit()`** + bandera `editTocado` (listener `input` en `#edit`) vuelcan los tramos tardíos al textarea sin pisar correcciones manuales. Con el texto de prueba real pasó de 0 a 18 rimas |

| Modos eliminados (12-ago) | Se borraron **Modo Bestia** y **Modo Principiante** por completo: una sola dificultad para todo el sitio. Fuera: bloque `.modos` con sus dos botones (`#bestiaBtn`/`#novatoBtn` y sus SVG), `<div class="brasas">` + el IIFE que la poblaba, el chip `.bestia-tag` del trainer, todo el CSS de `body.bestia-on`/`body.novato-on` (temas invertidos, `--tc` por tarjeta en 2 paletas extra, llamas `::after` en tcard/panel/verdict, burbujas), los keyframes `lamer`/`brasa`/`latido-infierno`/`flotar`/`subir-burbuja`, las variables `bestia`/`novato`, `pintarBestia()`, `guardarModo()`, `BADGE`, la persistencia en localStorage (`flBestia`/`flNovato`) y la línea de `newItem()` que pisaba `roundSeconds`. `SEGS` pasó de `{n,b,p}` a un número por técnica (los valores `n` de antes), `seg(k)` devuelve `SEGS[k]` y `palN(l)` ya no suma/resta. En Doble Tempo el objetivo quedó fijo en el 70% del doble tempo. Verificado: 17 tarjetas, cero errores de consola, y el reloj de cada técnica coincide con su insignia (multi 30, cadena 40, rimamin 60, tempo 60, sinrepetir 45, rutina 60, vocales 25) |

| Reordenamiento del menú (12-ago) | Las 17 tarjetas se reagruparon en 5 secciones nuevas pedidas por el usuario: **Técnicas básicas** (Rimas por Minuto, Sin Repetir, Sin Muletillas), **Técnicas profesionales** (Multisílabas, Métricas, Camaleón, Doble Tempo, Aliteración, Doble Sentido), **Modalidades** (Temática Obligada), **Entrenamiento educativo** (Trabalenguas, Resistencia de Exhalación, Cadena de Rimas, Encadenado Silábico), **Calentamiento previo** (Mandíbula, Espejo de Vocales, Rutina Pre-Audiencia). Los antiguos encabezados por familia (Rima/Voz/Ingenio/Disciplina/Cuerpo) desaparecieron. Las clases `fam-*` siguen en cada tarjeta porque controlan la tipografía y el `.tcard-motif` — **ojo**: el chip `.tcard-tag` de cada tarjeta todavía dice la familia vieja, que ya no coincide con el encabezado de su sección. Se añadió el `nth-of-type(17)` que faltaba en la entrada escalonada. Sin Muletillas, Encadenado Silábico y Rutina Pre-Audiencia no venían en la lista del usuario: se ubicaron por afinidad y quedan pendientes de confirmar |

| Corrección (13-ago) | Primer intento mal interpretado: se aplicó la máquina de escribir a las palabras de las **bocas**. El usuario la quería en la **batalla de abajo**. Las bocas se devolvieron exactas a su estado anterior (10 spans estáticos, Anton, `--fs` 20-34px, `cp-va-der`/`cp-va-izq`) y el tipeo se movió a `cicloBatalla` |
| Limpieza del menú (13-ago) | (1) Se eliminó el **banner de descarga de beats** (`.pistas-banner` + su CSS + el listener de `#pistasBtn`); el modal `#pistasOverlay` y `renderPistas()` siguen en el archivo porque las pistas se usan como fondo al grabar, pero **ya no hay forma de abrirlo desde la interfaz**. (2) El **globo de la batalla** perdió marco, fondo, sombra, padding y la colita `::after`: el verso va directo sobre el fondo. (3) El verso de la batalla ahora se escribe **palabra a palabra** (`cicloBatalla` reescrito): Space Mono 13px, cada palabra se teclea letra a letra con cursor `_`, y al pasar de `MAX_EN_PANTALLA=5` la más antigua se disuelve con blur — ventana rodante de 5 palabras. Al terminar el verso la cola se disuelve en cascada y a los 2,6s entra el siguiente, alternando lado. `.battle-mc` tiene `min-height:58px` para que el menú no salte |

| Menú plegable (14-ago) | Las 5 secciones del menú ahora son **desplegables**, todas cerradas al entrar, cada una independiente (no acordeón exclusivo — pueden estar varias abiertas). `fam-head` pasó de `<div>` a `<button>` con `aria-expanded` y una flechita `.fam-chev` que rota; `fam-body` usa el truco `grid-template-rows: 0fr→1fr` para animar el alto sin medir con JS. `.cards-home` dejó de ser el grid de tarjetas (ahora es una columna de `.fam-group`); el grid de tarjetas por sección vive en `.fam-cards` (2 col en móvil, `auto-fill minmax(200px,1fr)` desde 900px). El stagger de entrada (`cardIn`) se movió de "una vez al cerrar el splash, 17 tarjetas" a "cada vez que se abre una sección, hasta 6 tarjetas" — se re-dispara en cada apertura porque la clase `.open` se quita y se vuelve a poner. Verificado: 5 grupos cerrados al cargar, apertura múltiple simultánea, cierre individual, clic en tarjeta abre la técnica igual que antes, sin scroll horizontal ni errores de consola, en móvil y escritorio |

| Más motion (14-ago) | Pasada marcada de animación en todo el sitio, pedida por el usuario ("más divertido y atractivo"): hover de tarjetas más punchy (GSAP: y -7→-11, scale 1.035→1.06, rotación inversa al --rot base), flecha "Entrar →" avanza al hover, palabra acento del hero (`accent-pulso`) respira+rota en loop, `fam-head` hover ilumina texto y agranda el punto+chevron, botón Grabar pulsa (`rec-pulso`) mientras graba, y nueva función `fadeInView()` da entrada con escala+rebote (`back.out`) al Menú y al Entrenador en cada cambio de vista — antes solo había fundido de salida, sin entrada. Todo nuevo respeta el interruptor global `prefers-reduced-motion:reduce` ya existente (`*{animation:none!important}`), verificado con el navegador de pruebas que lo fuerza |

| Tarjetas "sólidas y elegantes" (14-ago) | Pedido: mantener la fuente distinta por familia (Bungee/Righteous/Permanent Marker/Alfa Slab One/Anton, sin tocar), pero quitar la inclinación tipo sticker y la esquina despegada, dejando las tarjetas derechas y más sólidas. Se eliminó `--rot` (ya no hay ángulo aleatorio por tarjeta), `.tcard-peel` (CSS + el `forEach` en JS que la creaba) y sus referencias en `leerRot()`/`animateCardsIn()`/`wireCardHover()` (rotation ya no se anima, siempre 0). El fondo pasó de una línea de acento izquierda de 3px a una franja superior de 3px, el degradado de `--tc` subió de 8% a 13% mezclado con `--surface`, el borde ahora es `color-mix` con `--tc` al 22% (antes gris plano), y la sombra es más profunda y definida (`0 10px 28px` en reposo, `0 26px 50px` en hover, antes `0 6px 22px`/`0 20px 40px`). Verificado: 17 tarjetas con `matrix(1,0,0,1,0,0)` (cero rotación), cero `.tcard-peel` en el DOM, las 5 fuentes de familia intactas, hover reproduce escala+elevación sin inclinación, sin errores de consola |

| Fix: hover cortado en el menú plegable (14-ago) | Bug reportado con captura: al pasar el mouse, la tarjeta se agranda pero queda "recortada", fuera de su lugar. Causa: `.fam-body>.fam-cards{overflow:hidden}` (necesario para animar el alto del acordeón cuando está cerrado) se quedaba activo también con la sección ABIERTA, cortando la escala/elevación del hover. Fix de una línea: `.fam-group.open .fam-body>.fam-cards{overflow:visible}` — el recorte solo sirve mientras el alto es 0 |

| Logo oficial (14-ago) | El sitio nunca tuvo un logo-imagen: el nombre se dibujaba con texto/CSS en 2 lugares (`.home-brand` en la barra del menú, `.sp-brand` en el splash). El usuario pegó el arte nuevo (mic+rayo+parlante, estilo glitch) y pidió dejarlo oficial, sin el fondo cuadriculado de la imagen. Fuente: `C:\Users\asfura\Downloads\Gemini_Generated_Image_j3ki0xj3ki0xj3ki.jpg` (recorte de la esquina superior derecha, sin los íconos PNG/SVG/JPG). Quitar el fondo con `rembg`/u2net dio mal resultado (dejaba manchas negras de sombra) — se optó por chroma-key manual por luminancia (`max(R,G,B)`, rampa 100→165) que sí funciona porque el logo es blanco/rojo sólido sobre fondo oscuro uniforme; se recortó al bounding box con margen. Resultado: `marca/logo-oficial.png` (838×566, RGBA, 240KB tras optimizar). Reemplazó el texto en ambos lugares: `.home-brand` ahora es un `<img>`; el splash conserva el ciclo de 9 tipografías (pedido explícito de no tocarlo) pero al pulsar "Entrar" ya no aterriza en texto Helvetica — aterriza en `.sp-logo` (mismo keyframe `sp-lock` de golpe de escala). El archivo `marca/freestyle-lab-logos.png` (moodboard viejo) quedó sin usar, no se borró. Verificado con capturas reales: splash cicla igual, logo aparece limpio sin fondo al aterrizar, barra del menú con el logo, sin errores de consola. A pedido, se dobló el tamaño en ambos lugares: `.home-brand img` 34px→68px (42px→84px en escritorio ≥900px), `.sp-logo` tope 420px→840px (cap de ancho relativo bajado de 80vw a 92vw para no desbordar en móvil al doblar el tope en px). Se agregó además al final del menú (`.home-foot`, 44px, centrado, opacidad .7, después de las 17 tarjetas) y como marca de agua en cada técnica (`.tbar-logo`, 30px, esquina derecha de la barra del entrenador) |
| **BUG grave encontrado y corregido** (14-ago) | Al agregar `.home-foot` apareció un logo duplicado flotando sobre el entrenador. Causa real: un `</div>` sobrante y huérfano que ya existía en el archivo **antes** de esta sesión (línea ~1029, después del cierre del último `.fam-group`) cerraba de forma prematura el `<div class="app">` que envuelve TODO el sitio — desde ahí, `#home`, `#trainer` y todo lo demás quedaban técnicamente fuera de `.app`, viviendo como hijos directos de `<body>`. Era invisible hasta ahora porque el diseño no dependía de esa envoltura para verse bien, pero rompía la maquetación en cuanto algo (mi `.home-foot`) necesitaba centrarse respecto a `.app`/`#home`. Diagnosticado con un parser real (`html.parser.HTMLParser`) que rastreó la pila de etiquetas abiertas — no a ojo, el archivo es demasiado grande para eso. Verificado tras el fix: el documento completo balancea perfecto (cero mismatches, cero huérfanos, cero etiquetas sin cerrar al final) |

| Fix: títulos de sección desbordados en teléfono (14-ago) | Bug reportado: "los textos de los menús quedaron descuadrados en teléfono". Causa: `.fam-head-text{flex:0 0 auto}` fuerza al título de cada sección plegable a una sola línea sin importar el ancho disponible — en pantallas angostas, títulos largos como "Entrenamiento educativo · mecánica y aguante" (405px) no caben en los ~371px reales de un teléfono y se desbordan. El panel de pruebas de este entorno no baja de ~472px de ancho (piso propio de la herramienta, no del sitio), así que se armó un iframe de 375px real apuntando al sitio para medir de verdad. Fix de una línea, sugerido por el usuario ("letras más anchas... que si se pasan bajen un renglón"): `.fam-head-text{flex:1 1 auto;min-width:0}` — permite que el texto se achique y baje a 2 líneas en vez de desbordar. Verificado dentro del iframe: `scrollWidth` pasó de 472 (con 100px de desborde real) a 371, igual al `clientWidth` — cero desborde; los 5 títulos miden 290px de ancho y 28px de alto (2 líneas) en vez de desbordar en una sola |

| Rediseño trainer/modales (15-ago) | El menú tenía el look "sólido y elegante" (tarjetas con degradado tintado, borde con color, sombra profunda) pero el trainer/modales se habían quedado con el estilo plano viejo — pendiente anotado desde el 10-ago. Se llevó el mismo lenguaje a: `.panel` (transcripción en vivo, tintado en `--stage`), `.verdict` (puntaje final, tintado en `--gold`, coherente con que el score-head ya es dorado), `.info-card` (modal "¿Cómo funciona?", tintado en `--stage`). Limpieza de radios sueltos en px → tokens `--radius-sm/md/lg`: `.stat`, `.info-body .ex`, `.cam-host`, `.info-card` (desktop). `.auth-bar` se revisó y ya usaba el mismo lenguaje pill/borde — no necesitó cambios. Verificado con computed styles (el panel de capturas falló de nuevo): `.panel` y `.info-card` con degradado + borde tintado + radio 22px; `.verdict` con radio 22px + borde dorado; `.stat` con radio 10px; sin errores de consola |

| Feature: Circuito (15-ago) | Nueva feature elegida de una lista de ideas de mejora: encadenar 2-5 técnicas seguidas con descanso entre medio (opción "Modo Rutina" del usuario — renombrada **Circuito** para no chocar con la técnica ya existente "Rutina Pre-Audiencia"). Botón dorado "Arma tu Circuito" arriba del menú → picker (reusa el modal `.info-overlay`/`.info-card`) con las 17 técnicas como chips seleccionables, cada uno con el color `--tc` de su tarjeta. Al terminar cada ronda (`renderVerdict`, el único punto por el que pasan las 17 técnicas incluidas las 3 de cámara), si hay un circuito activo se guarda el resultado y aparece una tarjeta "Siguiente: X" → "Descansar y seguir" → cuenta regresiva de 12s (o saltar) → pasa a la siguiente técnica **sin salir de #trainer** ni recargar nada. Al terminar la cola, resumen con el puntaje de cada técnica y botón "Volver al menú". Insignia "Circuito · 2/4" en la barra del entrenador mientras está activo. Refactor de paso: `openTech(tech)` se partió en `prepararTecnica(tech)` (arma el entrenador para una técnica, sin fundido de vista — reutilizable) + el fundido de vista; las 17 tarjetas pasaron a `data-tech="..."` y los 17 `addEventListener` sueltos se colapsaron en un solo bucle; los 15 `techName textContent="..."; newItem();` repetidos se colapsaron en un objeto `TECH_TITLES` (también usado por el picker, una sola fuente de verdad). Verificado end-to-end simulando `renderVerdict` para 3 rondas: cola se arma bien, resultados se acumulan, descanso cuenta y salta, insignia avanza 1/3→2/3→3/3, resumen final correcto, `circuito` vuelve a `null` y la insignia se esconde, "Volver al menú" funciona. También verificado que el flujo normal (fuera de circuito) sigue intacto: Doble Tempo, técnica de cámara (Mandíbula), clic directo en tarjeta limpia cualquier circuito abandonado. Capturas reales confirman el picker y la selección de chips. Cero errores de consola en toda la prueba |

## Checkpoints

<!-- checkpoint.sh añade líneas nuevas debajo -->
